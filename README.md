# DATA_221_Assignment_3

# Question_1.py
This file loads the crime dataset, selects the ViolentCrimesPerPop column, and then calculates and outputs the mean, median, standard deviation, minimum, and maximum values

# Question_2.py
This file loads the crime dataset, selects the ViolentCrimesPerPop column, initializes a Histogram and Boxplot using the data

# Question_3.py
This file loads the kidney disease dataset and sepeartes the input features from the target label. The data is split into training anf testing sets so the model can be trained

# Question_4.py
This file trains the KNN model using the kideny disease data and makes predications on the test set. It evalulates the model using confusion matric, precision, recall, accuracy and an F1 score

# Question_5.py
This file trains multiple KNN models using multiple values of k and calculates and stores the test accuracy for each one